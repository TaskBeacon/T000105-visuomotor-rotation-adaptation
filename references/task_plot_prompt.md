Use case: infographic-diagram
Asset type: TaskBeacon task flow diagram
Primary request: Create a clean, publication-ready task flow diagram as a timeline collection for the behavioral task described below.

Task: Visuomotor Rotation Adaptation
Construct: Sensorimotor adaptation / aftereffect
Rows/conditions:
- Baseline (30 trials): veridical white cursor follows the mouse and reaches the blue target.
- Early adaptation (first 10 of 54 rotation trials): the visible cursor path is rotated 45 degrees and initially shows a large target error.
- Late adaptation (last 10 of 54 rotation trials): compensatory mouse movement makes the rotated cursor path approach the target, leaving a smaller error.
- Aftereffect (6 trials): cursor disappears as movement begins; the dashed explanatory mouse path remains biased opposite the former rotation.

Timeline phases:
- Baseline: Home (self-paced; white cursor visible only within 2 degrees of centre; white start annulus) -> Hold (500 ms; no response; cursor inside start annulus) -> Reach (maximum 500 ms from movement onset; click-free mouse movement; blue target at 6 degrees and veridical white cursor path) -> Endpoint (50 ms; cursor frozen at target radius)
- Early adaptation: Home (self-paced; white cursor visible only within 2 degrees of centre; white start annulus) -> Hold (500 ms; no response; cursor inside start annulus) -> Reach (maximum 500 ms from movement onset; click-free mouse movement; blue target and white cursor path rotated 45 degrees away from the dashed mouse path, with a large target error) -> Endpoint (50 ms; rotated cursor frozen at target radius)
- Late adaptation: Home (self-paced; white cursor visible only within 2 degrees of centre; white start annulus) -> Hold (500 ms; no response; cursor inside start annulus) -> Reach (maximum 500 ms from movement onset; compensatory dashed mouse path opposite the rotation; rotated white cursor path approaches the blue target) -> Endpoint (50 ms; rotated cursor frozen near the target)
- Aftereffect: Direct instruction (before block; stop aiming and move directly to target) -> Home (self-paced; white cursor visible only within 2 degrees of centre; white start annulus) -> Hold (500 ms; no response; cursor inside start annulus) -> Reach (maximum 500 ms from movement onset; cursor hidden; blue target remains and dashed explanatory mouse path is biased opposite the former rotation) -> No feedback (movement endpoint; no cursor displayed)

Visual requirements:
- White background, landscape orientation, crisp dark text, restrained condition accent colors.
- One horizontal row per condition or representative trial type.
- Each row contains 4-5 participant-screen snapshots connected by a subtle arrow.
- Each screen snapshot shows the visible stimulus or feedback, not internal variable names.
- Use gray participant-screen boxes, thin black arrows, consistent row spacing, and subtle row separators.
- Place timing labels under each screen in compact text.
- Place condition labels at the left of each row.
- Use short labels only; avoid paragraphs inside the image.
- Make all text legible at normal document preview size.
- Use an upward blue target in every reach snapshot for a consistent comparison.
- Use solid white cursor traces only when cursor feedback is visible. A thin dashed orange line labelled `Mouse path` may be used as an explanatory overlay and must never be mistaken for visible cursor feedback.
- Leave a clean blank header band across the top 15-18% of the image. This band is reserved for a fixed title, `Construct: ...` subtitle, and TaskBeacon logo lockup that will be added after generation.

Accuracy constraints:
- Do not invent phases, stimuli, condition names, keys, rewards, or timings.
- Do not add people, lab equipment, decorative scenes, logos, or unrelated icons.
- Do not draw the task title, construct subtitle, any logo, watermark, brand mark, or `TaskBeacon` text inside the generated image.
- Draw only the timeline content below the blank header band.
- If a detail is unknown, omit it rather than guessing.
- Preserve these exact terms where used: Baseline, Early adaptation, Late adaptation, Aftereffect, Home, Hold, Reach, Endpoint, Direct instruction, No feedback, Mouse path, 500 ms, 50 ms, 30 trials, first 10, last 10, 6 trials, 45°.

Style:
TaskBeacon scientific infographic style: clean vector-like raster image, organized spacing, gray screen boxes, restrained color accents, and a blank header-safe area.

